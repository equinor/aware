{
  "checks": {
    "hungry": {
      "command": "/usr/bin/echo OK: I just ate, so I am fine && exit 0",
      "standalone": true,
      "handlers": ["default", "debug"],
      "interval": 60
    }
  }
}
